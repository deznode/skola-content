---
title: "Rezolve Problema i Rezumu"
language: kea
courseSlug: jdbc-postgresql
lessonSlug: troubleshooting-summary
objectives:
  - "Diagnostika i koriji os 5 falha komun di JDBC"
  - "Lembra takeaway prinsipal di tudu kursu"
  - "Aplika txeklista pa produson"
---

Kuandu bu konexon ka ta funsiona, normalmenti é un di sinku prublema. Tres primeru é di **konexon** — verifika-s rápidu ku komandus li baxu. Dipos, nu ta fitxa ku un rezumu di tudu kursu.

## Éru di konexon — diagnóstiku rápidu

<DetailDisclosure
  items={[
    {
      q: 'Éru: "No suitable driver found for jdbc:postgresql://…"',
      tag: "Classpath",
      a: "JAR di driver JDBC ka sta na bu classpath. Djunta dependénsia na `pom.xml` (Maven), ou djunta `.jar` manualmenti ku `-cp \".:postgresql-42.7.11.jar\"`.",
      code: "org.postgresql:postgresql:42.7.11",
    },
    {
      q: 'Éru: "Connection refused" ou "Connection timed out"',
      tag: "Konexon",
      a: "PostgreSQL ka ta kore, ou host/porta eradu. Konfirma ki servidor ta kore, ki porta é `5432`, i ki `pg_hba.conf` ta permiti konexon di bu IP.",
      code: "pg_isready",
    },
    {
      q: 'Éru: "FATAL: password authentication failed"',
      tag: "Otentikason",
      a: "Nomi di uzuáriu ou senha eradu. Testa kredensial diretu na `psql`, verifika éru di digitason na bu kódiku Java, i garanti ki uzuáriu ten permison `CONNECT` na bazi.",
      code: "psql -U bu_uzuariu -d bu_bazi",
    },
  ]}
/>

## Éru komun: SQL Injection

:::callout{type=danger}
Konkatena input di uzuáriu diretamenti na un string SQL ta dexa un atakanti inxeta komandu kumo `'; DROP TABLE uzuarius; --` i destrui bu dadus.
:::

<CodeDiff
  lang="java"
  filename="query di uzuáriu"
  title="Konkatenason (perigozu) vs PreparedStatement (seguru)"
  note="Ku `PreparedStatement`, input ta tratadu kumo **dadus**, nunka kumo kódiku SQL — asin `'; DROP TABLE uzuarius; --` ta bira un testu inofensivu, ka un komandu."
  diff={[
    { type: "del", t: "// PERIGOZU — nunka faze es!" },
    { type: "del", t: "String sql = \"SELECT * FROM uzuarius WHERE nomi_uzuariu = '\" + inputUzuariu + \"'\";" },
    { type: "add", t: "// SEGURU — sénpri uza PreparedStatement" },
    { type: "add", t: "String sql = \"SELECT * FROM uzuarius WHERE nomi_uzuariu = ?\";" },
    { type: "add", t: "PreparedStatement pstmt = conn.prepareStatement(sql);" },
    { type: "add", t: "pstmt.setString(1, inputUzuariu);  // input ta tratadu kumo dadus, nunka kumo kódiku" },
  ]}
/>

## Éru komun: Vazamentu di Rekursus

:::callout{type=danger}
Ka fitxa `Connection`, `Statement`, ou `ResultSet` ta sgota pool di konexon — i bu aplikasan ta krax ku "too many connections".
:::

```java
// SÉNPRI uza try-with-resources
try (Connection conn = DriverManager.getConnection(URL, USER, PASSWORD);
     PreparedStatement pstmt = conn.prepareStatement(sql);
     ResultSet rs = pstmt.executeQuery()) {

    // Trabadja ku resultadus

} // Tudu tres ta fitxadu automatikamenti li, mésmu si exsesan kontise
```

<SectionHeading variant="quiz">Verifika konhesimentu</SectionHeading>
<QuizSet showHeader={false}>
  <Quiz position={0} />
  <Quiz position={1} />
  <Quiz position={2} />
  <Quiz position={3} />
</QuizSet>

<SectionHeading variant="summary">Rezumu — kuzé ki bu prende</SectionHeading>
<KeyTakeaways showHeader={false}>
  <RezumuItem>Konekta Java ku PostgreSQL uzandu JDBC i `DriverManager`.</RezumuItem>
  <RezumuItem term="Tres objetu">`Connection`, `PreparedStatement`, i `ResultSet`.</RezumuItem>
  <RezumuItem>Faze tudu operasan CRUD di forma seguru ku `PreparedStatement` (`?` ta privini SQL injection).</RezumuItem>
  <RezumuItem>`ResultSet` ta funsiona kumo kursor — **ka** un koleson na memória.</RezumuItem>
  <RezumuItem>Transasan: `setAutoCommit(false)` / `commit()` / `rollback()` — tudu ou nada.</RezumuItem>
  <RezumuItem variant="gold" term="Regra di oru">Sénpri `PreparedStatement` ku `?`, sénpri try-with-resources.</RezumuItem>
</KeyTakeaways>

## Referénsia rápidu

```java
// Konekta
Connection conn = DriverManager.getConnection(
    "jdbc:postgresql://localhost:5432/nha_bazi", "uzuariu", "senha");

// Query ku parámetrus (SELECT)
PreparedStatement pstmt = conn.prepareStatement("SELECT * FROM uzuarius WHERE id = ?");
pstmt.setInt(1, uzuariuId);
ResultSet rs = pstmt.executeQuery();

// Modifika dadus (INSERT, UPDATE, DELETE)
pstmt = conn.prepareStatement("INSERT INTO uzuarius (nomi) VALUES (?)");
pstmt.setString(1, nomi);
int linhasAfetadus = pstmt.executeUpdate();
```

:::callout{type=tip}
Pa un referénsia kompletu di un pájina, odja cheatsheet di es kursu.
:::

## Seguransa i prósimus pasus

:::callout{type=warning}
**Nunka** guarda kredensial di bazi di dadus na kódiku fonti pa produson — uza variável di anbienti ou un jestor di segredu.
:::

```java
String url = System.getenv("DATABASE_URL");
String user = System.getenv("DATABASE_USER");
String password = System.getenv("DATABASE_PASSWORD");
```

<GlossaryText
  text="Pa produson, uza [[connection pooling]] pa reuza konexon en bez di abri un nobu kada bez, guarda kredensial fora di kódiku, i da bu uzuáriu di bazi sô [[least privilege]]."
  terms={{
    "connection pooling": { en: "connection pooling", definition: "Un **pool** ki ta mante un konjuntu di konexon abertu prontu pa reuza — más rápidu ki abri un nobu kada bez, i ta evita éru `too many connections`. `HikariCP` é padron di indústria.", code: "HikariDataSource ds = new HikariDataSource(config);" },
    "least privilege": { en: "least privilege", definition: "Da un uzuáriu di bazi sô permison mínimu ki el meste (p.e. só `SELECT`/`INSERT`, ka `DROP`) — pa limita danu si kredensial vaza." },
  }}
/>

<LessonObjectives
  eyebrow="Antis di produson"
  title="Txeklista pa produson"
  items={[
    "Connection pooling (`HikariCP`, `c3p0`, ou similar)",
    "Kredensial guardadu di forma seguru (variável di anbienti, jestor di segredu)",
    "Tudu query ta uza `PreparedStatement`",
    "Tudu rekursus fitxadu ku try-with-resources",
    "Tratamentu di éru i logging adekuadu",
    "Uzuáriu di bazi di dadus ku permison mínimu nesesáriu",
  ]}
/>

**Prósimus pasus:** aprofunda na transasan (isolation levels, batch), prende Connection Pooling ku [HikariCP](https://github.com/brettwooldridge/HikariCP), ou konstrui un REST API ki ta uza JDBC. Rekursus: [Docs JDBC di PostgreSQL](https://jdbc.postgresql.org/documentation/) · [Tutorial JDBC di Baeldung](https://www.baeldung.com/java-jdbc).
