---
title: "Operadoris"
language: kea
courseSlug: intro-python
lessonSlug: operadoris
slug: "operadoris"
module: 1
lesson: 8
duration: "20 min"
objectives:
  - "Uza operadoris aritmétiku (+, -, *, /, //, %, **) pa faze kálkulus"
  - "Kompara valoris ku operadoris di komparasan (==, !=, >, <, >=, <=)"
  - "Kombina kondisons ku operadoris lójiku (and, or, not)"
  - "Intende presedensia di operadoris i armadidjas komun"
---

# Operadoris

Operadoris é sinais ki ta dize Python pa faze un operasan ku valoris. Kuandu bu skrebe `10 + 5`, sinal `+` é operador i `10` ku `5` é operandus. Na es lisan, nos ta prende tres tipu prinsipal di operadoris: aritmétiku (pa kalkula), komparasan (pa kompara), i lójiku (pa kombina kondisons). Es é ferramentas fundamentál ki bu ta uza na tudu programa ki bu skrebe.

## Operadoris Aritmétiku

Es operadoris ta faze kálkulus matemátiku. Si bu sabe matemátika báziku, djá bu konxe maioria.

```python
# Adisan i Subtrasan
print(10 + 5)    # 15
print(10 - 3)    # 7

# Multiplikasan
print(4 * 3)     # 12

# Divisan (SENPRI retorna float!)
print(10 / 5)    # 2.0  ← nota: 2.0, KA 2!
print(7 / 2)     # 3.5
```

:::callout{type=warning}
Divisan ku `/` SENPRI ta retorna un float (númeru ku pontu desimal), mesmu kuandu rezultadu é "interu". `10 / 5` ta da `2.0`, ka `2`. Es é un armadidja komun!
:::

```python
# Divisan Interu (floor division) — tira parti desimal
print(7 // 2)    # 3  (ka 3.5 — tira desimal)
print(10 // 3)   # 3
print(21 // 5)   # 4

# Módulu (restu di divisan)
print(10 % 3)    # 1  (10 ÷ 3 = 3 restu 1)
print(15 % 5)    # 0  (divisan exata — sen restu)
print(7 % 2)     # 1  (ímpar: restu é 1)
```

```python
# Esponensasan (potensia)
print(2 ** 3)    # 8   (2 × 2 × 2)
print(5 ** 2)    # 25  (5 × 5)
print(10 ** 0)   # 1   (kualker númeru elevadu a 0 é 1)
print(9 ** 0.5)  # 3.0 (rais kuadrada!)
```

### Izemplu Prátiku: Kalkula Troku ku Eskudu

```python
# Kalkula troku na loja
presu = 750      # presu na Eskudus (ECV)
pagamentu = 1000 # klientu pagá ku nota di 1000

troku = pagamentu - presu
print(f"Troku: {troku} ECV")  # Troku: 250 ECV

# Kuantas notas di 100 i kuantas moedas?
notas_100 = troku // 100     # 2 notas di 100
restu = troku % 100          # 50 ECV sobra
print(f"{notas_100} nota(s) di 100 ECV i {restu} ECV na moeda")
```

### Izemplu Prátiku: Konversor di Moeda

```python
# Konversor Eskudu → Euro
eskudus = 5000
taxa_kambiu = 110.265  # 1 EUR = 110.265 ECV

euros = eskudus / taxa_kambiu
print(f"{eskudus} ECV = {euros:.2f} EUR")
# 5000 ECV = 45.35 EUR
```

### Par o Ímpar?

```python
# Truque ku módulu: verifica si númeru é par o ímpar
numeru = 17

if numeru % 2 == 0:
    print(f"{numeru} é par")
else:
    print(f"{numeru} é ímpar")
# 17 é ímpar
```

### Operadoris di Atribuisan Konposta

Bu pode kombina operadoris aritmétiku ku atribuisan pa atalhu.

```python
# Atalhu: operador + atribuisan
saldo = 1000

saldo += 500   # mesmu ki: saldo = saldo + 500
print(saldo)   # 1500

saldo -= 200   # mesmu ki: saldo = saldo - 200
print(saldo)   # 1300

saldo *= 2     # mesmu ki: saldo = saldo * 2
print(saldo)   # 2600

saldo //= 3    # mesmu ki: saldo = saldo // 3
print(saldo)   # 866
```

| Atalhu | Ekivalenti |
|--------|------------|
| `x += 5` | `x = x + 5` |
| `x -= 3` | `x = x - 3` |
| `x *= 2` | `x = x * 2` |
| `x /= 4` | `x = x / 4` |
| `x //= 2` | `x = x // 2` |
| `x %= 3` | `x = x % 3` |
| `x **= 2` | `x = x ** 2` |

<SectionHeading level={3} variant="practice">Tenta: Divisan Interu i Restu</SectionHeading>

<CodeCloze
  lang="python"
  title="Inche os spasus"
  prompt="Kalkula kuantas notas di 100 i kuantu sobra di un troku."
  template={[
    "troku = 250",
    "notas_100 = troku {{0}} 100   # 2 notas di 100",
    "restu = troku {{1}} 100       # 50 ECV sobra",
    'print(f"{notas_100} nota(s) i {restu} ECV")',
  ]}
  answers={["//", "%"]}
  hints={["Divisan interu (kosienti)", "Restu di divisan"]}
  solved="Korretu! `//` ta da o kosienti interu (2) i `%` ta da o restu (50)."
/>

## Operadoris di Komparasan

Es operadoris ta kompara dois valoris i ta retorna `True` o `False` (boolean). Es é essensial pa kondisionais (`if/elif/else`) ki bu ta prende na prósimu lisan.

```python
# Igual i Diferenti
print(10 == 10)   # True   (é igual?)
print(10 == 5)    # False
print(10 != 5)    # True   (é diferenti?)
print(10 != 10)   # False

# Maior i Menor
print(10 > 5)     # True
print(10 < 5)     # False
print(10 >= 10)   # True   (maior ó igual)
print(10 <= 9)    # False  (menor ó igual)
```

:::callout{type=warning}
Ka konfundi `=` (atribuisan) ku `==` (komparasan)! `x = 5` ta guarda valor 5 na `x`. `x == 5` ta pergunta "x é igual a 5?".
:::

<CodeDiff
  lang="python"
  filename="kompara_str.py"
  title="Komparasan di strings é case-sensitive"
  note="`==` ta distingui maiúskula di minúskula. Pa kompara sen liga pa kazu, baxa os dos ku `.lower()`."
  diff={[
    { type: "ctx", t: 'print("Praia" == "Praia")    # True' },
    { type: "del", t: 'print("Praia" == "praia")    # False — P ≠ p' },
    { type: "add", t: 'print("Praia".lower() == "praia".lower())  # True' },
  ]}
/>

```python
# Komparasan ku variáveis
idadi_joao = 25
idadi_ana = 30

print(f"João é más bedju ki Ana? {idadi_joao > idadi_ana}")   # False
print(f"Ana é más bedju ki João? {idadi_ana > idadi_joao}")    # True
print(f"Es tene mesmu idadi? {idadi_joao == idadi_ana}")       # False
```

### Izemplu Prátiku: Verifikasan di Idadi

```python
# Verifica si pesoa pode vota (>= 18 anu)
nomi = "Nilton"
idadi = 17

pode_vota = idadi >= 18
print(f"{nomi} pode vota? {pode_vota}")  # Nilton pode vota? False
```

## Operadoris Lójiku

Operadoris lójiku ta kombina vários kondisons. Pensa es kumo "i", "ó", "ka" na Kriolu.

### `and` — AMBOS ten ki ser True

```python
# and: tudu kondisons ten ki ser verdaderu
idadi = 25
tene_bilheti = True

print(idadi >= 18 and tene_bilheti)  # True (25 >= 18 i tene bilheti)
print(idadi >= 30 and tene_bilheti)  # False (25 ka >= 30)
```

### `or` — ALGUN ten ki ser True

```python
# or: pelo menus UN kondisan ten ki ser verdaderu
studanti = False
idosu = True

tene_diskontu = studanti or idosu
print(tene_diskontu)  # True (pelo menus un é True)
```

### `not` — inverte True/False

```python
# not: inverte valor lójiku
ta_xobe = True
print(not ta_xobe)  # False

loja_aberta = False
print(not loja_aberta)  # True
```

### Kombina Tudu Djuntu

```python
# Izemplu kompletu: validasan di bilheti
idadi = 22
studanti = True
tene_dinheru = True

# Pode entra si: (tene 18+ anu I tene dinheru) O é studanti
pode_entra = (idadi >= 18 and tene_dinheru) or studanti
print(f"Pode entra? {pode_entra}")  # True
```

```python
# Izemplu prátiku: validasan di senha
senha = "Py2026!"

tene_tamanhu = len(senha) >= 6
tene_numeru = any(c.isdigit() for c in senha)
tene_letra = any(c.isalpha() for c in senha)

senha_forte = tene_tamanhu and tene_numeru and tene_letra
print(f"Senha forte? {senha_forte}")  # True
```

<SectionHeading variant="concept">Tabela di Verdadi (Truth Table)</SectionHeading>

Pa intende `and` i `or`, kompara es dos lado-a-lado: `and` só ta da `True` kuandu **ambos** é `True`; `or` só ta da `False` kuandu **ambos** é `False`.

<CompareTable
  cornerLabel="A · B"
  showHeader={false}
  cols={[
    { name: "and", syntax: "A and B", accent: "blue" },
    { name: "or", syntax: "A or B", accent: "teal" },
  ]}
  rows={[
    { label: "`True` ku `True`", kind: "bool", vals: [true, true] },
    { label: "`True` ku `False`", kind: "bool", vals: [false, true] },
    { label: "`False` ku `True`", kind: "bool", vals: [false, true] },
    { label: "`False` ku `False`", kind: "bool", vals: [false, false] },
  ]}
/>

I `not` ta inverte un valor lójiku:

```python
# 'not' inverte True/False
print(not True)    # False
print(not False)   # True
```

## Presedensia di Operadoris (Orden di Exekusan)

![Presedensia di operadoris di Python](../../infographics/03-operador-precedensia.png)

Python ta sigi un orden espesífiku kuandu tene vários operadoris na mesmu expresan. É igualzinhu ki matemátika — multiplikasan antis di adisan.

```python
# Sen paréntesis: Python sigi presedensia
rezultadu = 2 + 3 * 4
print(rezultadu)  # 14 (ka 20! — * antis di +)

# Ku paréntesis: bo kontrola orden
rezultadu = (2 + 3) * 4
print(rezultadu)  # 20
```

**Orden di presedensia (di más altu pa más baxu):**

| Priuridad | Operador | Deskrisan |
|-----------|----------|-----------|
| 1 (más altu) | `**` | Esponensasan |
| 2 | `*`, `/`, `//`, `%` | Multiplikasan, divisan, módulu |
| 3 | `+`, `-` | Adisan, subtrasan |
| 4 | `==`, `!=`, `>`, `<`, `>=`, `<=` | Komparasan |
| 5 | `not` | Negasan lójika |
| 6 | `and` | E lójiku |
| 7 (más baxu) | `or` | Ó lójiku |

<AnnotatedCode
  lang="python"
  title="Kumo Python ta avalia 2 ** 3 + 4 * 2 - 1"
  code={[
    { t: "rezultadu = 2 ** 3 + 4 * 2 - 1", m: 1 },
    { t: "#           = 8 + 4 * 2 - 1", m: 2 },
    { t: "#           = 8 + 8 - 1", m: 3 },
    { t: "#           = 15", m: 0 },
    { t: "print(rezultadu)  # 15", m: 0 },
  ]}
  notes={[
    { m: 1, title: "Esponensasan primeru", body: "`**` ten a priuridadi más alta — `2 ** 3 = 8`." },
    { m: 2, title: "Dipos multiplikasan", body: "`*` ben antis di `+` i `-` — `4 * 2 = 8`." },
    { m: 3, title: "Pa fin, adisan i subtrasan", body: "Di skerda pa direta — `8 + 8 - 1 = 15`." },
  ]}
/>

:::callout{type=tip}
Komparasan ben antis di `and`/`or`/`not`. Pa kel motivu `idadi >= 18 and tene_bilheti` ka meste paréntesis — Python ta avalia `(idadi >= 18) and (tene_bilheti)`, ka `idadi >= (18 and tene_bilheti)`. I `not 5 > 3` é `not (5 > 3)` → `False`.
:::

:::callout{type=tip}
Na dúvida, uza **paréntesis**! `(2 + 3) * 4` é más klaru ki depende di presedensia. Kódiku klaru é más importante ki kódiku kurtu.
:::

## Armadidjas Komun

```python
# ARMADIDJA 1: / senpri retorna float
print(type(10 / 5))   # <class 'float'> — 2.0, ka 2!
print(type(10 // 5))  # <class 'int'>   — 2 (si bo kre interu, uza //)

# ARMADIDJA 2: = vs ==
x = 5      # atribuisan (guarda valor)
x == 5     # komparasan (pergunta si é igual) → True

# ARMADIDJA 3: komparasan di string é case-sensitive
nomi = "Pedro"
print(nomi == "pedro")  # False!
print(nomi.lower() == "pedro")  # True (soluson)

# ARMADIDJA 4: divisan interu ku negativus
print(7 // 2)    #  3  ← normal
print(-7 // 2)   # -4  ← aredonda pa BAXU, ka pa zeru!
```

<SectionHeading variant="practice">Tenta Gosi</SectionHeading>
<TentaGosi showHeader={false} />

<SectionHeading variant="quiz">Testa bu Konhesimentu</SectionHeading>
<QuizSet showHeader={false}>
  <Quiz position={0} /><Quiz position={1} /><Quiz position={2} /><Quiz position={3} />
</QuizSet>

<SectionHeading variant="summary">Rezumu</SectionHeading>
<KeyTakeaways showHeader={false}>
  <RezumuItem variant="gold" term="Regra di oru">Na dúvida sobri a orden, uza **paréntesis** — `(2 + 3) * 4` é más klaru ki konfia na presedensia. Kódiku klaru ganha di kódiku kurtu.</RezumuItem>
  <RezumuItem term="Aritmétiku">`/` ta da senpri float; `//` é divisan interu; `%` é o restu; `**` é potensia.</RezumuItem>
  <RezumuItem term="Komparasan">`==`, `!=`, `>`, `<`, `>=`, `<=` ta da senpri `True`/`False`.</RezumuItem>
  <RezumuItem term="Lójiku">`and` (os dos `True`), `or` (algun `True`), `not` (inverti).</RezumuItem>
  <RezumuItem term="Presedensia">`**` > `* / // %` > `+ -` > komparasan > `not` > `and` > `or`.</RezumuItem>
  <RezumuItem variant="warning" term="Errus kumuns">Ka konfundi `=` (atribuisan) ku `==` (komparasan). I komparasan di string é **case-sensitive** — `"Pedro" == "pedro"` é `False`.</RezumuItem>
</KeyTakeaways>
