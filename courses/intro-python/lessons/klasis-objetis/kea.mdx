---
title: "Klasis i Objetis"
language: kea
courseSlug: intro-python
lessonSlug: klasis-objetis
slug: "klasis-objetis"
module: 4
lesson: 26
duration: "25 min"
objectives:
  - "Kria klasis ku __init__ i uza self pa defini variáveis di instánsia"
  - "Defini métodus di instánsia ki ta opera sobri dadus di objetu"
  - "Modela entidadis di mundu real ku klasis (ezemplu: KontaBankáriu)"
  - "Kria vários objetis independenti a parti di mesmu klasi"
---

# Klasis i Objetis

Imagina reseta di katchupa. Reseta ta dize ingredientis (midju, feijun, mandioka, lingisa...), kantidadi, e pasus pa prepara. Ma reseta ka é katchupa — é só instruson. Kada bes ki bo sigui reseta i kuzinha, bo ta kria un **katchupa real** — un pratu diferenti, ku si sabor própriu.

Na programasun, **klasi** é komu reseta — un moldi ki ta deskrebe kumu un koisa debe ser. **Objetu** é kel ki bo ta kria a parti di moldi — un instánsia real, ku si dadus própriu. Bo pode uza mesmu reseta pa faze katchupa pa almósu na Praia i pa janta na Mindelo — dos pratu diferenti, mesmu reseta!

## Kria Klasi ku `class`

Pa defini un klasi na Python, bo uza keyword `class` seguidu di nomi (ku **PascalCase** — primeiru letra di kada palavra maiúskula):

```python
# Klasi más simples posível
class Pesoa:
    pass  # 'pass' signifika "nada aki por agora"

# Kria un objetu (instánsia) di klasi Pesoa
maria = Pesoa()
joao = Pesoa()

print(type(maria))  # <class '__main__.Pesoa'>
print(type(joao))   # <class '__main__.Pesoa'>
```

Es klasi ka ta faze nada inda — é komu un reseta na branku. Vamus adisiona ingredientis!

## `__init__` — Konstrutor di Klasi

Métudu espesiál `__init__` ta roda **automátikamenti** kuandu bo kria un objetu novu. É konstrutor — ta inisializa atributus di objetu:

```python
class Pesoa:
    def __init__(self, nomi, ilha, idadi):
        self.nomi = nomi      # Atributu: nomi di pesoa
        self.ilha = ilha      # Atributu: ilha di orijem
        self.idadi = idadi    # Atributu: idadi

# Kria objetis — __init__ ta roda automátikamenti
maria = Pesoa("Maria", "Santiago", 25)
nilton = Pesoa("Nilton", "São Vicente", 30)

print(maria.nomi)    # Maria
print(nilton.ilha)   # São Vicente
print(maria.idadi)   # 25
```

```python
# ERRU KOMUN: skese di pasa argumentus
# pesoa = Pesoa()
# TypeError: __init__() missing 3 required positional arguments
```

> **💡 Dika:** `__init__` ka é teknikamenti "kria" objetu — Python ta kria el primeiru, depois `__init__` ta inisializa si atributus. Nomi `__init__` signifika "initialize" (inisializa).

## `self` — O ki é?

`self` é referénsia pa **objetu atuál** — kel ki ta ser kriadu o usadu. Pensa na `self` komu "**nha**" (meu/nha). Kuandu bo skrebe `self.nomi = nomi`, é komu dize "nha nomi é esi valór":

```python
class Estudanti:
    def __init__(self, nomi, kursu):
        # "nha nomi é [nomi ki entra]"
        self.nomi = nomi
        # "nha kursu é [kursu ki entra]"
        self.kursu = kursu

# Kuandu Python ta kria 'ana', self = ana
ana = Estudanti("Ana", "Engenharia")
# ana.nomi → "Ana" (nha nomi)
# ana.kursu → "Engenharia" (nha kursu)

# Kuandu Python ta kria 'kelvin', self = kelvin
kelvin = Estudanti("Kelvin", "Medisina")
# kelvin.nomi → "Kelvin"
# kelvin.kursu → "Medisina"
```

```python
# ERRU KOMUN: skese di 'self' komu primeiru parámetru
class Mal:
    def __init__(nomi):  # ← falta self!
        nomi = nomi      # ← isu ka ta guarda nada na objetu!

# TypeError: __init__() takes 1 positional argument but 2 were given
```

> **💡 Dika:** `self` é **sempri** primeiru parámetru di tudu métudu dentu di un klasi. Python ta pasa el automátikamenti — bo ka presiza skrebe el kuandu ta xama métudu.

## Variáveis di Instánsia

Variáveis di instánsia é atributus ki **pertense a kada objetu individualmenti**. Kada objetu tene si própriu kópia:

```python
class Livru:
    def __init__(self, títulu, autor, presiu):
        self.títulu = títulu
        self.autor = autor
        self.presiu = presiu    # Presu na ECV

# Kada livru é independenti
livru1 = Livru("Python pa Tudu Jenti", "Amilcar", 1500)
livru2 = Livru("Algoritmus na Kriolu", "Cesária", 2000)

print(livru1.títulu)   # Python pa Tudu Jenti
print(livru2.títulu)   # Algoritmus na Kriolu

# Muda un ka ta afeta otru
livru1.presiu = 1200
print(livru1.presiu)   # 1200
print(livru2.presiu)   # 2000 (ka muda!)
```

```python
# Defini tudu atributu dentu di __init__
class Artista:
    def __init__(self, nomi, jeneru):
        self.nomi = nomi
        self.jeneru = jeneru
        self.albuns = []        # Lista vázia — ta enxi depois
        self.fans = 0           # Kumesa na zero

cesaria = Artista("Cesária Évora", "Morna")
print(cesaria.albuns)   # []
print(cesaria.fans)     # 0
```

> **💡 Dika:** **Sempri** defini tudu atributu dentu di `__init__`, mesmu si valór inisiál é `[]`, `0`, o `None`. Defini fora di `__init__` pode kauza `AttributeError` na otrus objetis.

## Métodus di Instánsia

Métodus é funsoens definidu **dentu di klasi** ki ta opera sobri dadus di objetu. Es ta resebe `self` komu primeiru parámetru:

```python
class Musiku:
    def __init__(self, nomi, instrumentu):
        self.nomi = nomi
        self.instrumentu = instrumentu
        self.músikus_tokadu = []

    def toka(self, músika):
        """Adisiona músika na lista di tokadu"""
        self.músikus_tokadu.append(músika)
        print(f"🎵 {self.nomi} ta toka '{músika}' na {self.instrumentu}")

    def repertóriu(self):
        """Mostra tudu músikus tokadu"""
        if not self.músikus_tokadu:
            print(f"{self.nomi} inda ka toka nada")
            return
        print(f"Repertóriu di {self.nomi}:")
        for i, m in enumerate(self.músikus_tokadu, 1):
            print(f"  {i}. {m}")

# Kria músiku i uza si métodus
joao = Musiku("João", "Violun")
joao.toka("Sodadi")           # 🎵 João ta toka 'Sodadi' na Violun
joao.toka("Petit Pays")       # 🎵 João ta toka 'Petit Pays' na Violun
joao.toka("Nha Testamentu")   # 🎵 João ta toka 'Nha Testamentu' na Violun
joao.repertóriu()
# Repertóriu di João:
#   1. Sodadi
#   2. Petit Pays
#   3. Nha Testamentu
```

```python
# Métudu ki ta retorna valór
class Retángulu:
    def __init__(self, largura, altura):
        self.largura = largura
        self.altura = altura

    def área(self):
        """Kalkula área di retángulu"""
        return self.largura * self.altura

    def perímetru(self):
        """Kalkula perímetru di retángulu"""
        return 2 * (self.largura + self.altura)

    def deskrebe(self):
        """Retorna deskrisun di retángulu"""
        return f"Retángulu {self.largura}x{self.altura} (área: {self.área()} m²)"

terenu = Retángulu(20, 15)
print(terenu.área())        # 300
print(terenu.perímetru())   # 70
print(terenu.deskrebe())    # Retángulu 20x15 (área: 300 m²)
```

## Ezemplu Prátiku: Konta Bankáriu

Vamus kria un klasi `KontaBankáriu` konpletu ku depózitu, levantamentu, e estrátu:

```python
class KontaBankáriu:
    def __init__(self, titulár, sáldu=0):
        self.titulár = titulár
        self.sáldu = sáldu          # Sáldu na ECV
        self.istóriku = []           # Istóriku di transasoens

    def depozita(self, montanti):
        """Depozita dinheru na konta"""
        if montanti <= 0:
            print("❌ Montanti debe ser pozitivu!")
            return
        self.sáldu += montanti
        self.istóriku.append(f"+{montanti} ECV (depózitu)")
        print(f"✅ {montanti} ECV depozitadu. Sáldu: {self.sáldu} ECV")

    def levanta(self, montanti):
        """Levanta dinheru di konta"""
        if montanti <= 0:
            print("❌ Montanti debe ser pozitivu!")
            return
        if montanti > self.sáldu:
            print(f"❌ Sáldu insufisienti! Bo tene {self.sáldu} ECV")
            return
        self.sáldu -= montanti
        self.istóriku.append(f"-{montanti} ECV (levantamentu)")
        print(f"✅ {montanti} ECV levantadu. Sáldu: {self.sáldu} ECV")

    def estrátu(self):
        """Mostra estrátu di konta"""
        print(f"\n{'='*40}")
        print(f"  Konta di {self.titulár}")
        print(f"{'='*40}")
        if not self.istóriku:
            print("  (sem transasoens)")
        else:
            for transasun in self.istóriku:
                print(f"  {transasun}")
        print(f"{'─'*40}")
        print(f"  Sáldu atuál: {self.sáldu} ECV")
        print(f"{'='*40}\n")
```

```python
# Maria ta abri konta na Praia
konta_maria = KontaBankáriu("Maria", 50000)
konta_maria.depozita(15000)   # ✅ 15000 ECV depozitadu. Sáldu: 65000 ECV
konta_maria.levanta(8000)     # ✅ 8000 ECV levantadu. Sáldu: 57000 ECV
konta_maria.depozita(25000)   # ✅ 25000 ECV depozitadu. Sáldu: 82000 ECV
konta_maria.levanta(100000)   # ❌ Sáldu insufisienti! Bo tene 82000 ECV

konta_maria.estrátu()
# ========================================
#   Konta di Maria
# ========================================
#   +15000 ECV (depózitu)
#   -8000 ECV (levantamentu)
#   +25000 ECV (depózitu)
# ────────────────────────────────────────
#   Sáldu atuál: 82000 ECV
# ========================================
```

## Kria Vários Objetis

Kada objetu ki bo kria é **totalmenti independenti**. Muda un ka ta afeta otru — mesmu ki es sai di mesmu klasi:

```python
# Kria vários kontas independentis
konta_maria = KontaBankáriu("Maria", 50000)
konta_pedro = KontaBankáriu("Pedro", 30000)
konta_djina = KontaBankáriu("Djina", 75000)

# Operasoens na un konta ka ta afeta otrus
konta_maria.depozita(10000)   # Maria: 60000 ECV
konta_pedro.levanta(5000)     # Pedro: 25000 ECV
konta_djina.depozita(20000)   # Djina: 95000 ECV

# Kada un tene si sáldu própriu
print(f"Maria: {konta_maria.sáldu} ECV")   # Maria: 60000 ECV
print(f"Pedro: {konta_pedro.sáldu} ECV")   # Pedro: 25000 ECV
print(f"Djina: {konta_djina.sáldu} ECV")   # Djina: 95000 ECV
```

```python
# Más un ezemplu: Lista di estudantis
class Estudanti:
    def __init__(self, nomi, ilha, nota):
        self.nomi = nomi
        self.ilha = ilha
        self.nota = nota

    def aprovadu(self):
        """Verifika si estudanti pasa (nota >= 10)"""
        return self.nota >= 10

    def deskrebe(self):
        status = "✅ Aprovadu" if self.aprovadu() else "❌ Reprovadu"
        return f"{self.nomi} ({self.ilha}) — Nota: {self.nota} — {status}"

# Kria lista di estudantis
turma = [
    Estudanti("Ana", "Santiago", 16),
    Estudanti("Nilton", "São Vicente", 8),
    Estudanti("Edna", "Sal", 14),
    Estudanti("Nelson", "Fogo", 11),
    Estudanti("Ivete", "Boa Vista", 9),
]

# Mostra tudu
print("📋 Rezultadus di Turma:")
for e in turma:
    print(f"  {e.deskrebe()}")

# Filtra aprovadus
aprovadus = [e for e in turma if e.aprovadu()]
print(f"\n{len(aprovadus)} di {len(turma)} estudantis aprovadu!")
# 3 di 5 estudantis aprovadu!
```

> **💡 Dika:** Klasis é perfeitu pa modela kualker koisa di mundu real — pesoas, produtus, kontas, veíkulus, enkomendas di Sukupira... Pensa na ki **dadus** i ki **asoens** kel koisa tene.

## Errus Komunis

```python
# ERRU 1: Skese di self na métudu
class Mal:
    def __init__(self, nomi):
        self.nomi = nomi

    def saudason():  # ← FALTA self!
        print(f"Ola, N é {self.nomi}")

# p = Mal("João")
# p.saudason()  # TypeError: saudason() takes 0 positional arguments but 1 was given
```

```python
# ERRU 2: Defini atributu fora di __init__
class Mal:
    def __init__(self, nomi):
        self.nomi = nomi

    def seta_idadi(self, idadi):
        self.idadi = idadi  # Só ezisti si seta_idadi() foi xamadu!

p = Mal("Maria")
# print(p.idadi)  # AttributeError: 'Mal' object has no attribute 'idadi'
# Pamodi? Pamodi seta_idadi() nunka foi xamadu!
```

```python
# KORETU: Defini tudu na __init__
class Bon:
    def __init__(self, nomi, idadi=None):
        self.nomi = nomi
        self.idadi = idadi  # Ezisti sempri, mesmu si é None

p = Bon("Maria")
print(p.idadi)  # None (ka da erru!)
```

## Tenta Agora 🏋️

1. **Exersísiu 1 — Klasi Produtu:** Kria un klasi `Produtu` ku atributus `nomi`, `presu` (na ECV), e `stoki`. Adisiona métodus `vende(quantidade)` (ki ta reduz stoki si tene sufisienti) e `repon(quantidade)` (ki ta aumenta stoki). Testa ku produtus di Sukupira.

2. **Exersísiu 2 — Klasi Restoránti:** Kria un klasi `Restoranti` ku atributus `nomi`, `sidadi`, e `menu` (disionáriu ku nomi di pratu i presu). Adisiona métudu `adisiona_pratu(nomi, presu)`, `mostra_menu()`, e `fazi_pedidu(lista_pratus)` ki retorna total na ECV.

3. **Exersísiu 3 — Klasi Jogador:** Kria un klasi `Jogador` ku atributus `nomi`, `ekipa`, `golus` (kumesa na 0), e `jogus` (kumesa na 0). Adisiona métodus `joga(golus_marcadu)` i `média_golus()`. Kria 3 jogadoris i mostra ken ki tene midjor média.

4. **Exersísiu 4 — Klasi BibliotekaBásiku:** Kria un klasi `Biblioteka` ku un lista di livrus (kada livru é un disionáriu ku 'títulu', 'autor', 'disponível'). Adisiona métodus `adisiona_livru()`, `empresta_livru(títulu)`, e `devolve_livru(títulu)`. Testa ku livrus di autoris kaboverdianu.

## Rezumu

- **Klasi** é un moldi (reseta) ki ta deskrebe kumu kria objetis; defini ku `class NomiKlasi:`
- **Objetu** é un instánsia real kriadu a parti di klasi (ezemplu: `maria = Pesoa("Maria", "Santiago", 25)`)
- **`__init__`** é konstrutor ki ta roda automátikamenti kuandu objetu é kriadu; inisializa atributus
- **`self`** é referénsia pa objetu atuál ("nha") — sempri primeiru parámetru di métodus
- **Variáveis di instánsia** (`self.nomi`) pertense a kada objetu individualmenti
- **Métodus di instánsia** é funsoens dentu di klasi ki ta opera sobri `self`
- **Kada objetu é independenti** — muda un ka ta afeta otru
- **Defini tudu atributu na `__init__`** — evita `AttributeError`

---

**Prósimu lisun:** [Eransa (Simples i Múltiplu)] →
